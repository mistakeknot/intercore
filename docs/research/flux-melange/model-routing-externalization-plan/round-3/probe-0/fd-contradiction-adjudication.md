# contradiction-adjudication — round 3

## Findings Index
- [P1] posed-contradiction-is-a-mis-pairing-real-one-lies-deeper — the two findings handed in for adjudication (drift-report tie-break vs. ignore-with-warning sunset) target different mechanisms and were never in tension; the actual contradiction (f-007 vs f-013, both anchored to the *same* Risks/Phase-1 line) was already adjudicated in round-2/probe-0 and still holds against current repo state (§Risks line 121, §Phase 1 line 58)
- [P2] drift-report-tie-break-gap-still-open-independent-of-the-pairing — claim (1) (Phase 6 registry-vs-interrank tie-break) is real, ungrounded-in-any-contradiction, and unresolved by round 2's f-007/f-013 verdict, which only fixed the Risks-line degradation policy, not Phase 6's separate collation step (§Phase 6 lines 99-104)

## Findings

### posed-contradiction-is-a-mis-pairing-real-one-lies-deeper
- **Severity:** P1
- **Where:** §Risks line 121, §Phase 1 line 58 (claim 2's location); §Phase 6 lines 99-104 (claim 1's location) — two different sections of the plan
- **What:** The two findings given to me as "a contradiction to adjudicate" are not actually opposed to each other. Claim (1) is fd-scribal's `registry-refresh-drift-report-has-no-adjudication-path` (P2): Phase 6's registry-vs-interrank drift report names no tie-break rule. Claim (2) is fd-coppice's `f-013`, verbatim (P3, `cluster_id: c-degrade-policy-no-sunset-ratchet`, `disagreement_refs: ["f-007"]`): the Risks-line "ignore-with-warning on unknown versions" policy has no sunset/escalation ratchet. These describe two different mechanisms — one is a *data-reconciliation* rule (which of two capability-value witnesses wins), the other is a *schema-version-mismatch* degrade policy (what an adapter does when it can't parse a `routing.yaml` it wasn't built for). Nothing in the plan text or the heat ledger puts these in tension: I checked `heat-ledger.jsonl` and claim (1)'s actual ledger entries (f-004, f-016, cluster `c-drift-report-disposition`) both carry `disagreement_refs: []` — never flagged against anything. The real, ledger-confirmed contradiction sharing claim (2)'s exact location is f-013 vs. **f-007** (fd-pilotage-authority-boundary, P1, `cluster_id: c-version-degrade-open-on-safety`, `disagreement_refs` cross-referenced both ways), which argues the *same* ignore-with-warning line degrades open against the plan's own fail-closed trust-zone guarantee (line 37) — a genuinely different and higher-stakes claim than claim (2)'s "needs a sunset clause." That pair was already adjudicated in `round-2/probe-0/fd-contradiction-adjudication.md`, in favor of f-007 (field-class split as primary fix, f-013's ratchet layered on top for the non-safety path only). I independently re-verified the grounding this round: `grep -rn "version\|Version" internal/routing/*.go` still returns zero real version-field matches (only the unrelated `InferCategoryExported` doc-comment on `resolve.go:159`); `internal/routing/config.go` exists but has no version-check logic; the routing-overrides PRD's actual version contract (`docs/prds/2026-02-15-interspect-routing-overrides.md` F1) still reads "proceed without exclusions" — a fail-open-toward-more-review precedent, not one that licenses fail-open on a fail-closed trust boundary. Nothing in the repo has moved since round 2; the verdict stands unchanged.
- **Evidence:** `heat-ledger.jsonl` — f-004/f-016 (claim 1's actual source lineage) both show `disagreement_refs: []`; f-013 (claim 2's verbatim source) shows `disagreement_refs: ["f-007"]`, confirming the *intended* pairing is f-007↔f-013, not claim(1)↔claim(2). `grep -rn "version\|Version" internal/routing/*.go` (this session): only `resolve.go:159` comment, zero code. Plan line 37 ("Fail closed. Encodes the argus-adjacent discipline mechanically") and line 121 (blanket ignore-with-warning) confirmed unchanged and still juxtaposed with no field-level carve-out. `round-2/probe-0/fd-contradiction-adjudication.md` verdict re-read in full: f-007 wins on grounding (borrowed precedent's blast radius is bounded by design; `routing.yaml` is not) and priority (a sunset ratchet on a uniform policy still leaves the trust-zone window live for versions 1..N-1).
- **Suggestion:** Two separate actions, not one adjudication: (a) confirm round 2's f-007-wins verdict as final for the Risks-line/Phase-1 degradation policy — Phase 1's contract spec should add the field-class-aware acceptance bullet already drafted in round 2 ("safety fields fail closed, non-safety fields warn-and-degrade with a bounded staleness window"); no new work needed here beyond carrying that fix into the next plan revision. (b) Treat claim (1)'s Phase 6 tie-break gap as a fully independent, still-open P2 — it is not resolved by fixing the Risks-line policy, because Phase 6's drift report is a different code path (registry-refresh script cross-checking interrank) with its own missing adjudication rule. Apply fd-scribal's own suggested fix there: state a presumption ("interrank wins for `benchmark`-tagged fields; registry wins for `judgment`-tagged fields; both shown side-by-side") in Phase 6's acceptance criteria.

### drift-report-tie-break-gap-still-open-independent-of-the-pairing
- **Severity:** P2
- **Where:** §Phase 6 lines 99-104: "Add a registry-refresh script cross-checking registry cost/benchmark values against interrank, flagging drift (does not auto-write; provenance stays explicit)." Acceptance: "drift report runs clean against seed registry."
- **What:** Once claim (1) and claim (2) are correctly disentangled (see above), claim (1) stands on its own merits and I re-confirm it as live: Phase 6 is, as fd-scribal noted, the plan's only genuine two-witness collation step (hand-maintained registry vs. interrank snapshot), and it is specified only down to "flagging drift" — no default presumption for which witness is trusted pending human review, and no distinction offered between "registry is stale," "interrank is stale," and "same model name, different deployment being compared." This is a real, separate gap from the Risks-line schema-version question, and it is not touched by round 2's f-007/f-013 resolution: fixing "safety fields fail closed, non-safety fields warn-and-degrade" for `routing.yaml` version mismatches says nothing about what a human triaging a *drift report* should presume when the registry and interrank disagree about, say, Sol's discernment score. The gap fd-scribal's suggested fix targets (tie-break by provenance tag: interrank wins on `benchmark`-tagged fields, registry wins on `judgment`-tagged fields) is a clean, cheap, one-sentence Phase 6 acceptance-criteria addition and should not get lost by being bundled into the (separate, already-resolved) version-degradation discussion.
- **Evidence:** Lines 99-104 (Phase 6, full section) contain no adjudication language for drift disagreement. `heat-ledger.jsonl` f-004 and f-016 confirm this is a distinct, previously-unadjudicated cluster (`c-drift-report-disposition`) with no disagreement references to any other finding, including f-007/f-013.
- **Suggestion:** Add to Phase 6's acceptance criteria: "Drift report states a default tie-break (interrank presumed correct for `benchmark`-tagged registry fields; hand-maintained registry presumed correct for `judgment`-tagged fields) and flags same-name/different-deployment mismatches as a distinct category from value drift."

## Verdict
Not a real contradiction as posed — a mis-pairing. The two findings handed to me for adjudication target different mechanisms (Phase 6 data-reconciliation vs. Risks-line schema-version degradation) and the ledger confirms neither was ever flagged against the other (`disagreement_refs: []` on both of claim (1)'s source entries). The genuine contradiction sharing claim (2)'s exact location is f-007 vs. f-013 — already adjudicated in `round-2/probe-0/fd-contradiction-adjudication.md` in favor of f-007's safety/non-safety field-class split, and I independently re-verified that grounding still holds against the current repo (no version field exists in `internal/routing`, the borrowed precedent is fail-open-toward-review not fail-open-toward-unconstrained-routing). That verdict should simply be carried forward, not re-litigated. Claim (1), read on its own terms, is an unresolved and legitimate P2: Phase 6's drift report still names no tie-break rule, and no other finding — including the f-007/f-013 resolution — closes that gap.
